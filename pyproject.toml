[project]
name = "bes"
version = "0.1.0"
description = "Bes is good"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "puremagic>=1.30",
    "python-magic>=0.4.27",
    "python-magic-bin>=0.4.14",
    "requests>=2.32.5",
    "xattr>=1.2.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.16.0",
    "pytest>=8.4.2",
    "pytest-xdist>=3.8.0",
]
